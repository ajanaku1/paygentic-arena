services:
  - type: web
    name: paygentic-arena
    runtime: node
    buildCommand: npm install && npm run seed && npm run build
    startCommand: npm start
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: LOCUS_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
